import React from 'react'

function Props(props) {
  return (
    <div>
      <div>
        <p>{props.popup}</p>
        <button>{props.button}</button>
      </div>
    </div>
  )
}

export default Props
